server:
  port: 10001
spring:
  profiles:
    active: dev
  application:
    name: Game
  jmx:
    default-domain: Game
eureka:
  instance:
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 7

